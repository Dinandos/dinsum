{
  "fields": [
    {
      "type": "networks",
      "key": "services.nginx.networks",
      "label": "Service Netwerken",
      "description": "Welke netwerken moet de nginx container gebruiken?",
      "default": ["proxy"]
    },
    {
      "type": "networks",
      "key": "networks",
      "label": "Netwerk Definities",
      "description": "Definieer de eigenschappen van de netwerken (extern/intern)",
      "default": {
        "proxy": { "external": true }
      }
    }
  ]
}